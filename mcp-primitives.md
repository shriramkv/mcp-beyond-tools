# MCP Primitives at a Glance

Tools are executable actions with side effects. Resources are read-only
context. Prompts are reusable templates. Most servers over-use tools and
ignore the other two, which bloats context and weakens auditability.
